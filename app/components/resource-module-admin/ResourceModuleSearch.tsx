import React from 'react';
import { Search } from '~/components/common/Search';

const ResourceModuleSearch = () => {
    return <Search label="Søk etter brukere med rolle" id={'search-user-role'} />;
};

export default ResourceModuleSearch;
